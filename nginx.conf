# Define user and worker processes (default values)
user  nginx;
worker_processes  auto;
pid        /var/run/nginx.pid;

# Main configuration
events {
    worker_connections  1024;
}

http {
    # Set up logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Server Block for reverse proxy
    server {
        listen 8080;  # Listening on port 8080

        # Proxy for Jenkins
        location /jenkins/ {
            proxy_pass http://localhost:8081/;  # Jenkins runs on port 8081
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Proxy for Your Application
        location / {
            proxy_pass http://localhost:8082/;  # Your app runs on port 8082
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
